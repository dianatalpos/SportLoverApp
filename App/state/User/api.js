import request from 'superagent';
import { Api, myLocalhost } from '../../constants/api';

const login = (user) => new Promise((resolve, reject) => {
});

const register = (user) => new Promise((resolve, reject) => {
   
});

const getUserById = (user) => new Promise((resolve, reject) => {

});

export default {
    login,
    register,
    update,
    getUserById
};