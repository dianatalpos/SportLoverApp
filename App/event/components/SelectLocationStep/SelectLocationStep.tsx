import React from "react";
import { Text, View } from "react-native";

const SelectLocationStep = ({ locations, onSelect }) => {
    return (
        <View></View>
    );
};

SelectLocationStep.defaultProps = {
    locations: [],
    onSelect: () => {},
};

export default SelectLocationStep;
